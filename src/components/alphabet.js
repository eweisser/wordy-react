import '../App.css';
import AIMenuButton from './AI-menu-button.js';

const Alphabet = ({children}) => {

    return (
        <div id="alphabet">
            {children}
        </div>
    );
}

export default Alphabet;